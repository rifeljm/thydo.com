import { hot } from 'react-hot-loader/root';
import React from 'react';

// import css from '../css/App.css';

class App extends React.Component {
  state = {
    dates: [],
  };

  render() {
    return <div>Spontan</div>;
  }
}

export default hot(App);
